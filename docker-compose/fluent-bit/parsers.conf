[PARSER]
    Name        ordermesh_retry_service
    Format      regex
    # /var/log/ordermesh-retry/<service>/<file>.log → service 라벨
    Regex       ^/var/log/ordermesh-retry/(?<service>[^/]+)/.+$
